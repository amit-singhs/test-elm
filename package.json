{
  "name": "test-elm",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run clean && cd public && npx parcel build --cache-dir ../.cache -d ../dist index.html",
    "start-old": "npx elm-live src/PortsPos.elm --hot --start-page=public/index.html -- --debug --output=public/main.js",
    "start": "cd public && npx parcel -d ../dist --cache-dir ../.cache index.html",
    "clean": "rm -rf ./dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/amit-singhs/test-elm.git"
  },
  "author": "Amit",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/amit-singhs/test-elm/issues"
  },
  "homepage": "https://github.com/amit-singhs/test-elm#readme",
  "devDependencies": {
    "@psf/bch-js": "^4.18.4",
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.4",
    "node-elm-compiler": "^5.0.6",
    "parcel-bundler": "^1.10.3"
  },
  "dependencies": {
    "elm-live": "^3.4.1"
  }
}
